{
  "name": "constellation-agent",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Constellation 에이전트 서버 — Claude Agent SDK 를 assistant-ui 데이터 스트림으로 잇고 도구 호출을 웹뷰에 중계한다",
  "scripts": {
    "start": "node src/server.ts",
    "test": "node --test src/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.274",
    "@hono/node-server": "^2.1.1",
    "assistant-stream": "^0.3.43",
    "hono": "^4.13.8",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/node": "^22.20.3",
    "typescript": "^7.0.2"
  }
}
