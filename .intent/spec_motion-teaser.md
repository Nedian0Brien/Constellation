---
title: JS 모션그래픽 30초 티저
slug: motion-teaser
stage: spec
status: accepted
intent: .intent/intent_motion-teaser.md
date: 2026-09-24
---

# JS 모션그래픽 30초 티저 — 명세

## 요구사항

- [ ] `video/motion/video.html` 하나로 재생되고, `render.mjs video`의 마지막 줄이 `OK`다. 출력은 1920×1080, 30fps, 30초이고 AAC 오디오가 붙는다.
- [ ] 이야기(자막은 한국어, 장면마다 한 가지만 말한다):

| # | 시간 | 화면 | 자막 | 전달하는 것 |
|---|---|---|---|---|
| 1 | 0–4초 | 흩어진 점 16,554개가 떠 있다(논문 하나가 점 하나) | 논문 16,554편 / 로봇 학습 · 체화 AI · 자율주행, 2014–2026 | 무엇을 넣는가 |
| 2 | 4–10초 | 점들이 실제 지도 좌표로 날아가 모이고, 분야마다 색이 들고 상위 분야 이름이 켜진다 | 비슷한 연구끼리 모여 분야 지도가 된다 | 무엇이 나오는가(지도) |
| 3 | 10–14초 | Robot Learning으로 다가가 RT-1이 떠오르고 제목이 붙는다 | 분야에서 논문 한 편까지 | 탐색 |
| 4 | 14–19초 | RT-1에서 참조 34개(들어오는 선)와 피인용 135개(나가는 선)가 뻗는다. 숫자가 센다 | 무엇을 인용했고, 누가 인용했는지 | 무엇이 나오는가(인용) |
| 5 | 19–26초 | 실제 질문이 입력되고, 에이전트의 도구 호출이 흐른 뒤 지도에 라벨 세 개(실제 기록)가 붙는다 | 물으면 에이전트가 지도에서 찾아 준다 | 무엇을 할 수 있는가 |
| 6 | 26–30초 | 지도가 물러나며 로고와 제품명 | Constellation / 논문으로 연구 분야의 지도를 그리는 탐색 도구 | 한 줄 정의 |

- [ ] 데이터는 모두 실제 값이다. `video/motion/build-data.mjs`가 스냅샷(`video/public/data/`)과 대화 기록에서 `data.js`를 만든다.
  - 담는 것: 좌표·클러스터·연도, 상위 분야 이름과 중심, RT-1 인용 이웃, 라벨이 붙는 세 논문의 제목, 질문과 도구 호출 이름
  - 수치(16,554편, 34, 135, 연도 범위)는 이 파일에서 읽어 그린다. 영상 코드에 숫자를 적지 않는다.
- [ ] 스타일 프레임 `video/motion/frames/{a,b,c}.html`은 모두 같은 시점(t=16, 인용선 장면)의 대표 장면을 그린다.
  - a: 고요·정밀
  - b: 역동·웅장
  - c: 깔끔·제품적
  - 팔레트, 서체, 재료, 움직임 문법, 구도 가운데 3개 이상이 서로 다르다.
- [ ] 사용자가 방향을 고른 뒤에만 `video.html`을 만든다.
- [ ] 결정성: `render.mjs video.html determinism <t>`가 같다는 결과를 낸다.
- [ ] 스킬 규칙: 미적 값은 `STYLE`에만 둔다. `Math.random()`·`Date.now()`를 쓰지 않는다.

## 변경(사용자 결정, 2026-09-24)

- **방향:** 세 스타일 프레임 가운데 하나를 고르는 대신, 사용자가 새 방향을 정했다. 논문 노드로 이루어진 우주를 카메라가 날아다니며 탐색하는 연출이다. 논문 16,554편을 실제 3D 좌표(3D UMAP 투영의 x·y·z)에 별로 띄운다.
- **자막:** 설명 문장 자막은 쓰지 않는다. 위 표의 자막 열을 대체한다.
  - 제목은 제품명 **Constellation** 하나다(처음과 끝).
  - 화면의 다른 글자는 데이터에 있는 사실만 둔다: 논문 편수, 분야 이름, RT-1 제목, 인용 수, 실제 질문, 에이전트가 쓴 도구 이름과 라벨.
- **확대 다이얼:** 앱의 화면 확대 다이얼(ZoomDial) 디자인을 화면 오른쪽에 넣고, 카메라가 다가간 만큼 배율이 올라간다.
- **2D 연구 지도로 전환(사용자 결정):** 3D 별자리는 앱의 중심 화면이 아니므로, 앱의 기본 화면인 2D 연구 지도를 기준으로 다시 만든다. 위의 3D 우주 방향을 대체한다.
  - 노드와 지도 요소는 앱 디자인을 그대로 옮긴다: 주제 색 점, 상위 피인용 흰 테두리, 영역 배경 그러데이션, 영역 이름(Georgia), 논문 제목(모노 대문자 + 점 색 연도), 선택 고리, 참조 파랑·피인용 빨강 인용선, 에이전트 주석, 바탕 십자와 크롭 마크, 캡션, 확대 컨트롤.
  - 앱의 지도 함수(`frontend/src/views/map`)를 묶어 그대로 부른다. 영역 이름 배치, 제목이 켜지는 배율, 점 크기, 색이 앱과 같은 코드에서 나온다.
  - 카메라는 앱처럼 확대·이동만 한다(정사영). 질문과 도구 호출은 앱 대화의 사용자 메시지·도구 행 모양으로 보인다.

## 설계

- **데이터 압축(`data.js`, `window.DATA`)**
  - 좌표는 지도 범위를 0–10000 정수로 양자화한 배열이다.
  - 클러스터, 연도, 상위 분야 번호도 정수 배열로 둔다.
  - 상위 분야는 트리 레벨 0(원래 절단 8개)을 쓴다. 이름 짓기가 지도용으로 가른 레벨이 아니라 `hierarchy`의 원래 절단이다. 이름이 붙은 노드가 없으면 레벨 1 이름을 쓴다(구현에서 확인).
  - 영상 파일 옆에 두고 `<script src>`로 읽는다. 크기는 약 300KB이고 커밋한다(OpenAlex 데이터, CC0). 그래야 HTML만 열어도 재생된다.
- **대화 기록:** 화면에 쓰는 것은 질문 전문, 도구 호출 이름 순서, `annotate` 라벨 세 개다. 모두 기록 그대로 쓴다. 긴 답(845자)은 쓰지 않는다. 대신 라벨이 붙는 것으로 답을 보인다.
- **추출:** 스킬의 `render.mjs`(헤드리스 Chrome → ffmpeg)로 한다. 결과는 `video/motion/out/`(git 밖)에 둔다.

## 버린 대안

- **Remotion 코드를 고쳐 연출만 바꾸기:** 앱 컴포넌트를 재사용하는 구조라 다시 앱 화면 쪽으로 끌려간다. 사용자가 JS 방식을 골랐다.
- **앱 스크린샷이나 앱 화면 재현:** 이전 버전의 문제 그 자체다.

## 함정

- 16,554개 점을 매 프레임 그리면 입자 시스템이 무거울 수 있다. 재생이 느려도 추출은 프레임 단위라 결과에는 영향이 없다. 재생 속도는 보고에 적는다.
- 웹폰트는 추출 중 네트워크로 받는다. `fonts`에 실제 문장을 넣어 로드를 기다린다.
- 인용선 135개와 34개를 동시에 그리면 화면을 덮는다. 뻗는 시간을 거리순으로 나눈다.

## 완료 기준

- 스타일 프레임 시트를 사용자에게 보여 주고 방향을 고르게 한다.
- 고른 방향으로 본 제작을 마친 뒤 다음을 확인한다.
  - `render.mjs video`의 마지막 줄이 `OK`다.
  - `stills` 시트로 장면 경계를 확인한다.
  - `determinism`이 같다는 결과를 낸다.
  - 스킬 `verify.md` 항목을 보고한다.
